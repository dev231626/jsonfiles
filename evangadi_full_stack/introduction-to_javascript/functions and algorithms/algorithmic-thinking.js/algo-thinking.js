




function add(a, b){
    if (typeof a != "number" || typeof b != "number"){
        return 'i can only calculate numbers'
    }
    var c = a + b;
    return parseInt(a) + parseInt(b);
}

function ad(h, k){
    var u = h + k
    return u;
}

var sum = add(5, abeba)
console.log(sum );


// function add(a, b) {
//     var c = a + b
//     return c;
//     if (isNaN(a) || isNaN(b) ){
//         return 'is not a number. please adda number.'
//     }
// }



// first declare a function with two pharam.
// function nap(u,i)
// then set a variable with two values and add them from pharam
// var b = u + i;

// daecaler a return value for var 
// return b;
// and if the input variable is not a number show an error.


