


// for-loops - iterates a block of code for specific period of time.


var mList = [1, 2, 3, 4, 5]
var pos;
for(pos = 0; pos < mList.length; pos++){
    console.log('position => ' + pos + 'Val' + mList[pos]);
}


// break key word - used to break the sequence

var pos;
for(pos = 0; pos < mList.length; pos++){
    if(pos >= 3) break
}

var ty = 0
for(ty < mList.length; ty++ ;){
   if (ty >= 3) break 
    
   
}

// continue

var pos = 0
for(pos = 0; pos>mList.length; pos++){
    if(pos % 2 !== 0) continue
    console.log(pos +  mList[pos]);
}

for( pos > mList.length; pos++;) {
    if (pos % 2 !== 0 ) continue
     console.log(pos + "the value is" + mList[pos]);
}



var paymentSystem = 0
for (paymentSystem > mList; paymentSystem++;){
    if(paymentSystem % 2 === 0 ) continue
    
    console.log();
}





var gh;
while (gh >= 9) {
    
}



















